fun main(args: Array<String>) {

    val popCorn = 5
    val hotDog = 7
    var customers = 10

    //some customer leave the queue

    //customers = 8
    println("There are ${customers} customers in the queue")

    //Now one more customer joins the queue
    println("There are ${customers + 1} customers in the queue")


    val name = "Mary"
    val age = 20

    println("${name}  is ${age} years old")


}